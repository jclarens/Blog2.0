#8 

#9 set  tag.blade #31--> <form role="form" action =" {{route('tag.store')}} " method="post" >
            {{csrf_field()}}
        #4 use App\Model\user\tag; TagController store --> 
        Category too
        create folder include/message.error, panggil di tag cate dan post 

FIX PROBLEM CSS DAN JQUERY GAK LOAD gara" kurang '/' di awal
